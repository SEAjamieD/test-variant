name: "Test"
run-name: "Test token"


on:
  workflow_dispatch:

jobs:
  check-token:
    runs-on: ubuntu-latest
    steps:
      - name: Code Checkout
        run: |
          echo "hello"
          echo ${{ secrets.GITHUB_TOKEN }}
